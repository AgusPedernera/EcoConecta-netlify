[build]
command = "npm install && npm run full"
publish = "client"   # La carpeta que contiene los archivos estáticos generados

[[redirects]]
from = "/api/*"
to = "http://localhost:8080/:splat"
status = 200
force = true

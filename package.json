{
  "scripts": {
    "full": "concurrently \"cd server && npm run server\" \"cd client && npm run dev\"",
    "build": "cd client && node_modules/.bin/vite build"
  },
  "dependencies": {
    "axios": "^1.6.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}